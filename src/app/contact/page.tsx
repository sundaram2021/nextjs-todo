import React, { FC } from "react";

interface IProps {};

const Contact:FC<IProps> = (props) => {
    return <div>
        <h1>Contact
            
        </h1>
    </div>
};

export default Contact;